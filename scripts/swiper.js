const swiper = new Swiper('.swiper', { 
    spaceBetween:25,
    slidesPerView: 'auto',
    freeMode: true,
    navigation: {
      nextEl: '#swiper-filter .swiper-button-next',
      prevEl: '#swiper-filter .swiper-button-prev',
    },
  
  });